import React, { useState } from 'react';

// Exercise: State with events (mirror the solution)
export default function Example1_Counter() {
  // TODO 1: Create count state starting at 0 using useState
  // Hint: const [count, setCount] = useState(0);

  return (
    <div>
      <h2>Hello CodeHub</h2>
      {/* TODO 2: Display Count: {count} */}
      {/* TODO 3: Add button to increment count using setCount */}
      {/* TODO 4: Add button to reset count to 0 */}
    </div>
  );
}


